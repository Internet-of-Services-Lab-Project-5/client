export type Passenger = {
  first_name: string;
  last_name: string;
  birthdate: string;
};

export type ResultEntry = {
  firstName: string;
  lastName: string;
  birthdate: string;
  wasUnruly: boolean;
};

export type Airline = {
  name: string;
  ethAddress: string;
  iExecAddress: string;
};
